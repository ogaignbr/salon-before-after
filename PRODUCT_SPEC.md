# ぴたカメ 商品仕様

## 概要

ぴたカメは、ユーザーが選択した画像をカメラ画面にゴースト表示し、その画像に構図を合わせて新しい写真を撮影できる比較カメラアプリです。

従来の「1枚目を撮影してから2枚目を合わせて撮る」ビフォーアフター撮影ではなく、端末内にある好きな画像を基準画像として選び、その画像にぴったり合わせて撮影できることを特徴とします。

美容サロンのビフォーアフターだけでなく、成長記録、姿勢記録、インテリア変更、商品撮影、作品記録、植物の観察、作業報告など、さまざまな場面で「同じ構図の比較写真」を作るために利用できます。

## コンセプト

好きな画像に、ぴたっと合わせて撮れるカメラ。

ぴたカメは、変化や違いを伝えるための写真を、誰でも簡単に同じ構図で撮影できるようにするアプリです。過去の写真、参考画像、理想の構図、記録したい対象の前回画像などを基準にして、今の状態を重ねながら撮影できます。

## 想定ユーザー

- 美容サロンで施術前後の変化を見せたい人
- 姿勢や体型の変化を記録したい人
- 子どもや植物などの成長を同じ構図で残したい人
- 部屋や店舗のインテリア変更を比較したい人
- 商品写真や作品写真を毎回同じ構図で撮りたい人
- 作業前後、清掃前後、修理前後などを記録したい人

## 基本方針

### アプリ内に画像を保存しない

ぴたカメは、アプリ内に画像記録を蓄積することを前提にしません。

ブラウザ更新、端末変更、キャッシュ削除などによって「保存していたはずの写真が消えた」と感じさせないため、撮影後はユーザーがその都度、必要な画像を端末へ保存または共有する仕様にします。

アプリは「撮影と出力のための道具」として動作し、画像管理アプリにはしません。

### その場で選び、その場で撮り、その場で出力する

基本の利用フローは以下です。

1. 端末内の画像を基準画像として選択する
2. 基準画像をゴースト表示した状態でカメラを起動する
3. 表示された基準画像に合わせて撮影する
4. 必要に応じて手動モザイク加工を行う
5. 撮影画像単体、または比較画像として保存・共有する

## 主要機能

### 基準画像の選択

ユーザーは撮影前に、端末のカメラフォルダや画像フォルダから基準画像を選択します。

選択された画像は撮影中だけ使用され、アプリ内の履歴として保存しません。

### ゴースト表示撮影

選択した基準画像をカメラ画面に半透明で重ねて表示します。

ユーザーは基準画像の位置や形に合わせて、今の状態を撮影できます。

ゴースト表示には以下の操作を用意します。

- ゴースト濃度の調整
- 表示しながら撮影
- 撮影前の基準画像変更

### ガイド表示

撮影時の構図合わせを補助するため、以下のガイドを表示できます。

- グリッド
- 三分割
- 斜め線

各ガイドはボタンで表示・非表示を切り替えられるようにします。複数のガイドを同時に表示できる仕様とします。

旧仕様にあった「顔」「体」の専用ガイドは削除します。

### 手動モザイク加工

撮影後、出力前に画像へ手動でモザイク加工を行えます。

対象は以下です。

- 基準画像
- 撮影画像

モザイク加工後の画像は、その場の出力に反映されます。

比較画像を出力する場合は、基準画像と撮影画像の両方について、加工後の状態を使用します。

撮影画像のみを保存する場合は、撮影画像へのモザイク加工だけが出力に反映されます。

### 出力

撮影後、ユーザーは以下の出力方法を選択できます。

#### 撮影画像のみ保存

今撮影した画像だけを保存・共有します。

基準画像は含めません。

#### 比較画像を保存

基準画像と撮影画像を並べた1枚の画像として保存・共有します。

比較画像には「Before」「After」などの文字やラベルは入れません。画像だけを並べます。

比較レイアウトは以下から選択できます。

- 左右比較
- 上下比較

左右比較の場合は、左に基準画像、右に撮影画像を配置します。

上下比較の場合は、上に基準画像、下に撮影画像を配置します。

## 画面構成

### ホーム画面

アプリの入口となる画面です。

主な操作は以下です。

- 撮影を開始する
- 利用規約やプライバシーポリシーを確認する

旧仕様の「記録を見る」は、アプリ内保存を前提としない新仕様では削除または非表示にします。

### 基準画像選択画面

撮影前に基準画像を選ぶ画面です。

主な要素は以下です。

- 基準画像選択ボタン
- 選択した基準画像のプレビュー
- 撮影開始ボタン
- 必要に応じた注意文

記録名、顧客名、顔・体の撮影部位選択は使用しません。

### 撮影画面

カメラ映像に基準画像をゴースト表示し、構図を合わせて撮影する画面です。

主な要素は以下です。

- カメラ映像
- 基準画像のゴースト表示
- ゴースト濃度スライダー
- グリッド切替
- 三分割切替
- 斜め線切替
- カメラ切替
- シャッターボタン
- 戻るボタン

### 出力画面

撮影後に画像を確認し、モザイク加工や保存方法を選択する画面です。

主な要素は以下です。

- 撮影画像のプレビュー
- 基準画像の確認
- 基準画像をモザイク加工
- 撮影画像をモザイク加工
- 撮影画像のみ保存
- 左右比較で保存
- 上下比較で保存
- 撮り直し
- 基準画像を選び直す

## 保存に関する考え方

ぴたカメは、画像をアプリ内に長期保存する仕様にはしません。

ユーザーに対しては、撮影後に必要な画像を端末へ保存・共有する使い方を推奨します。

これにより、アプリ更新やブラウザ環境の変化によるデータ消失トラブルを避けます。

## 旧仕様から削除するもの

- Before撮影ステップ
- After撮影ステップ
- アプリ内の撮影履歴保存
- 記録一覧
- 顧客名入力
- 顔・体の撮影部位選択
- 顔・体専用ガイド
- 比較画像上のBefore/Afterラベル

## 旧仕様から残すもの

- カメラ撮影
- 基準画像のゴースト表示
- ゴースト濃度調整
- カメラ切替
- 手動モザイク加工
- 画像の保存・共有
- 利用規約、プライバシーポリシー

## 実装方針

### データ管理

基準画像と撮影画像は、撮影から出力までの一時的な状態として扱います。

IndexedDBなどに画像を保存する既存の記録機能は、新仕様では基本的に使用しません。

### 画像出力

撮影画像のみの保存では、撮影画像のBlobをそのまま保存・共有します。

比較画像の保存では、基準画像と撮影画像をCanvas上で合成して、1枚の画像として出力します。

### モザイク加工

既存の手動モザイク加工機能は流用し、対象を「Before/After」から「基準画像/撮影画像」に変更します。

加工後の画像は出力用の一時データとして保持し、アプリ内履歴には保存しません。

## 商品としての一言説明

ぴたカメは、好きな画像を半透明で重ねながら、同じ構図の写真を簡単に撮影できる比較カメラです。

