# ぴたカメ 商品仕様

## 概要

ぴたカメは、ビフォー（基準）とアフター（現在）を同じ構図で撮影し、左右または上下に並べて比較・出力できるカメラアプリです。**写真と動画の両方**に対応し、変化や違いをひと目で伝えるための素材をその場で作成できます。

主な利用フローは3つです。

1. **ビフォーを撮影** — 基準となる写真・動画を撮る
2. **アフターを撮影** — ビフォーを片側に表示しながら、同じ構図でアフターを撮る
3. **ビフォーアフターを並べる** — 2つの素材を選び、並べて1枚の画像／1本の動画として出力する

PIN認証とStripe課金によるサブスクリプション制で提供します。

## コンセプト

好きな画像・動画に、ぴたっと合わせて撮れるカメラ。

ぴたカメは、変化や違いを伝えるための写真・動画を、誰でも簡単に同じ構図で撮影できるようにするアプリです。過去の素材、参考映像、理想の構図、記録したい対象の前回の状態などを基準にして、今の状態を並べながら撮影・比較できます。

## 想定ユーザー

- 美容サロンで施術前後の変化を見せたい人
- フード撮影で同じ構図の写真・動画を撮りたい人
- 商品写真や作品写真を毎回同じ構図で撮りたい人
- 作業前後、清掃前後、修理前後などを記録したい人
- スポーツのフォーム比較など、動きの違いを並べて確認したい人
- 子どもや植物などの成長を同じ構図で残したい人

## 基本方針

### アプリ内に素材を永続保存しない

ぴたカメは、アプリ内に画像・動画を永続的に蓄積しません。

撮影・生成した素材はセッション中のメモリ上で保持します。ブラウザを閉じると消えるため、必要な素材はその都度、端末へ保存または共有します。

### その場で撮り、その場で並べ、その場で出力する

基本の利用フローは以下です。

1. ログインしてホーム画面に入る
2. ビフォー／アフターを撮影、または端末の素材を選択する
3. 並べる画面でレイアウト・フレーム・グリッド・（動画は）トリミングを調整する
4. 現在の表示状態のまま端末へ保存・共有する

## 写真モードと動画モード

ビフォー撮影・アフター撮影・並べる画面のすべてで、**写真モードと動画モードを切り替え**できます。両モードは同じUI・同じ操作感で扱えます。

### 写真モード

- カメラのライブ映像からシャッターで静止画を撮影
- 撮影後はプレビュー表示 →「撮り直し」または「端末に保存」

### 動画モード

- 録画ボタン（赤）をタップで録画開始／停止
- 録画中は赤い点滅インジケーターと経過時間を表示
- 録画停止後は `<video>` でプレビュー再生 →「撮り直し」または「端末に保存」
- 音声は録音しません（映像のみ）
- 録画形式は端末のサポートに応じて `video/mp4` → `video/webm（vp9）` → `video/webm` の順に自動選択

グリッドは撮影・録画中のプレビュー上にオーバーレイ表示され、保存される素材自体には焼き込まれません。

## 認証

### PIN認証

ユーザーはメールアドレスと4桁のPINコードでログインします。

初回登録時のPINは「0000」が設定され、初回ログイン後にPIN変更を強制します。

PIN忘れの場合は、メールアドレスを入力してPINリセットを行えます。

認証セッションはlocalStorageに保存し、再訪時の自動ログインに対応します。

### 認証バックエンド

認証処理はSupabase Edge Functionsで実行します。

- `pin-auth` - ログイン、セッション検証、PIN変更
- `reset-pin-by-email` - PINリセット

## サブスクリプション

### 料金体系

- 7日間の無料トライアル
- トライアル後は月額980円

### Stripe連携

サブスクリプション管理にStripeを使用します。

- 新規登録時にStripe Checkoutへリダイレクト
- 支払い管理用のカスタマーポータルへのリンク
- サブスクリプション状態の取得・更新

### サブスクリプション状態

以下の状態を管理します。

- `trialing` - トライアル中
- `active` - 有効
- `canceled` - キャンセル済み
- `past_due` - 支払い遅延
- `expired` - 期限切れ
- `none` - 未登録

有効またはトライアル中の場合のみ、撮影・比較機能にアクセスできます。

## カメラ機能

- フロントカメラとリアカメラの切り替え
- 高解像度（理想値 3840x2160）を要求し、ズームは標準倍率（1.0）に固定
- クロップせずカメラの元映像をそのまま撮影し、プレビューと距離感を一致させる
- フロントカメラ使用時は左右反転補正を適用
- 撮影時のフラッシュエフェクト（写真モード）
- グリッドオーバーレイの表示切替（なし／白／赤）

撮影プレビューの枠はアスペクト比3:4で表示します。

## グリッド

構図合わせ用のグリッドを、各画面で「なし → 白 → 赤」の3段階で切り替えられます。視認性を高めるため、線を濃く・本数を抑えた設定にしています。

| 場所 | 間隔 | 白 | 赤 |
|---|---|---|---|
| 撮影プレビュー（CSS） | 40px | rgba(255,255,255,0.4) | rgba(255,0,0,0.55) |
| 並べる画面プレビュー | 3列 × 10行 | rgba(255,255,255,0.55) | rgba(239,68,68,0.7) |
| 出力（Canvas描画） | 80px | rgba(255,255,255,0.45) | rgba(239,68,68,0.7) |

## 主要機能

### ビフォー撮影（写真／動画）

カメラ単体でビフォー素材を撮影する画面です。

- 写真／動画モード切替
- グリッド切替（なし／白／赤）
- カメラ切替
- 撮影・録画 → プレビュー → 端末に保存

### アフター撮影（写真／動画）

ビフォーを片側に表示しながら、同じ構図でアフターを撮影する画面です。

- 画面を左右に分割表示
  - 左側：ビフォー素材（端末から選択）
  - 右側：カメラのライブ映像 → アフターを撮影
- **写真モードでは左側にビフォー画像を選択**、**動画モードでは左側にビフォー動画を選択**（モードに応じて選択できる種別が切り替わる）
- ビフォー動画には再生／停止（▶／⏸）ボタンがあり、任意の瞬間で止めて構図を合わせられる
- ビフォー素材はピンチズーム・ドラッグ・ズームボタン（-/+）・リセットで位置と倍率を調整可能
- グリッド切替（なし／白／赤）
- アフターを撮影・録画 → プレビュー → 端末に保存

### ビフォーアフターを並べる（写真／動画）

2つの素材を並べて1枚の画像／1本の動画として編集・出力する画面です。

#### モード切替

- 写真モード：2枚の画像を選んで並べる
- 動画モード：2本の動画を選んで並べる（左に動画・右に動画）
- モードを切り替えると選択中の素材はクリアされ、ファイル選択も画像／動画に自動で切り替わる

#### レイアウト

- 横並び（左右比較）
- 縦並び（上下比較）

#### アスペクト比

- 3:4
- 9:16
- 4:3

#### フレーム設定（折りたたみパネル）

- ボーダーの有効/無効
- ボーダー幅
- ボーダー色（プリセット6色＋カラーピッカー）
- 仕切り線の幅
- 角丸（ボーダー半径）

#### 素材操作

各素材について個別に以下を調整できます。

- タップで選択（選択中はハイライト表示：1枚目=緑／2枚目=青）
- ドラッグによる位置調整
- ピンチズーム・ズームボタン（-/+）による拡大縮小
- リセット

#### 動画の再生・トリミング（動画モード）

長さの違う動画でも揃えて比較できるよう、トリミング機能を備えています。

- 再生（▶）／停止（⏸）／最初へ（⏮）ボタンで2本の動画を同時に制御
- 1枚目・2枚目それぞれに開始秒・終了秒のスライダーがあり、切り抜き範囲を指定できる
- 再生はトリミング終了時刻で自動停止
- 出力時はトリミング範囲だけが録画され、長さを揃えた比較動画になる

#### グリッド表示

並べる画面上にグリッド（なし／白／赤）を重ねて表示できます。

#### 手動モザイク加工（写真モードのみ）

各画像に対して個別にモザイク加工を行えます。

- 円形ブラシによる塗り操作
- ブラシサイズの調整（10〜80px）
- モザイク強度の調整（ブロックサイズ5〜40px）
- 元に戻す機能（Undo）
- タッチ操作とマウス操作の両対応

### 出力

#### 表示状態のまま保存

出力は **現在の画面の表示状態をそのまま反映** します。グリッドを表示していればグリッドあり、非表示にしていればグリッドなしで保存されます。グリッドの色（白／赤）もそのまま出力に反映されるため、保存内容は「なし／白／赤」から選べます。

保存ボタンは1つで、現在のグリッド状態を文言に表示します（例：`保存（グリッドなし）`／`保存（白グリッド）`／`保存（赤グリッド）`）。

#### 編集画面と出力の一致

出力はセル内に素材全体を収める contain-fit で描画し、編集画面のプレビュー（`object-contain`）と同じ見え方・倍率で保存されます。

#### 画像の出力

- Canvas上で合成し、幅1080pxベースのJPEG（品質92%）として生成
- Navigator Share API（モバイル）またはダウンロードリンクで保存・共有

#### 動画の出力

- 2本の動画をCanvasに描画し、`canvas.captureStream` + MediaRecorder で1本の比較動画として合成
- 生成中は進捗（%）を表示
- 生成後は**プレビューモーダル**を表示。ユーザーの新しいタップで保存・共有を発火させることで、iOSでの保存失敗（長時間処理後にユーザー操作の有効性が切れる問題）を回避
- モーダル内のプレビューは長押しからの「ビデオを保存」にも対応

## 画面構成

### ログイン画面（/）

- メールアドレスとPINコードによるログイン
- PIN変更モード（初回ログイン時に強制）
- PIN忘れリセットモード
- 新規登録画面、マニュアル、利用規約、プライバシーポリシーへのリンク

### 新規登録画面（/signup）

- メールアドレス入力
- Supabase Edge Function経由でStripe Checkoutへリダイレクト

### サブスクリプション画面（/subscribe）

- 登録完了メッセージの表示
- トライアル情報と料金の説明
- Stripe Checkout開始ボタン
- サブスクリプション状態の更新・トライアル残日数の表示
- 支払い管理ポータルへのリンク

### ホーム画面（/home）

- ロゴとユーザー情報の表示
- 「ビフォーを撮影」「アフターを撮影」「ビフォーアフターを並べる」の3ボタン
- ユースケースカード（6種類：美容／フード／商品撮影／作業報告／作品記録／成長・観察）
- 利用規約、プライバシーポリシーへのリンク
- ログアウトボタン

### ビフォー撮影画面（/capture-before）

- 写真／動画モード切替
- グリッド切替・カメラ切替
- 撮影・録画 → プレビュー → 端末に保存

### アフター撮影画面（/capture-after）

- 左右分割（左：ビフォー素材／右：カメラ）
- 写真／動画モード切替（モードに応じてビフォーの選択種別も切替）
- ビフォー動画の再生／停止
- ビフォーのピンチズーム・ドラッグ・ズーム・リセット
- グリッド切替・カメラ切替
- アフター撮影・録画 → プレビュー → 端末に保存

### 並べる画面（/compare）

- 写真／動画モード切替
- レイアウト切替（横並び／縦並び）
- アスペクト比切替（3:4／9:16／4:3）
- フレーム設定パネル（折りたたみ式）
- 各素材のドラッグ・ズーム操作
- グリッド切替（なし／白／赤）
- 動画モード：再生／停止／最初へ、1枚目・2枚目のトリミングスライダー
- 写真モード：各画像のモザイク編集
- 保存ボタン（現在の表示状態で出力）
- 動画は生成後にプレビューモーダルから保存・共有

### マニュアル画面（/manual）

- 目次ナビゲーション、画像付きの操作説明、スムーズスクロール対応

### 利用規約画面（/terms）／プライバシーポリシー画面（/privacy）

- それぞれの文書を表示

## ルーティングとアクセス制御

### 公開ページ

- `/` - ログイン
- `/signup` - 新規登録
- `/subscribe` - サブスクリプション
- `/manual` - マニュアル
- `/terms` - 利用規約
- `/privacy` - プライバシーポリシー

### 認証必須ページ（AuthGuard）

以下のページはAuthGuardで保護され、アクセスには次の条件が必要です。

- ログイン済みであること
- 初期PIN変更が完了していること
- サブスクリプションが有効（active）またはトライアル中（trialing）であること

保護対象ページ：

- `/home` - ホーム
- `/capture-before` - ビフォー撮影
- `/capture-after` - アフター撮影
- `/compare` - ビフォーアフターを並べる

条件を満たさない場合、ログイン画面またはサブスクリプション画面へリダイレクトします。

## 技術スタック

### フロントエンド

- React 19 + TypeScript 6
- React Router v7
- Tailwind CSS v4
- Vite + vite-plugin-pwa

### バックエンド

- Supabase（認証・Edge Functions）
- Stripe（課金）

### 状態管理

- React Context（AuthContext、SessionContext）
- localStorage（認証セッション永続化）
- メモリ上の素材管理（セッション中のみ）

### カメラ・録画

- `useCamera` フック：getUserMediaによるカメラ制御、写真キャプチャ、カメラ切替、ストリーム提供
- `useVideoRecorder` フック：MediaRecorderによる録画（映像のみ）、経過時間管理、録画Blob生成

### 画像・動画処理（imageProcessor）

- `createComparisonImage`：Canvasによる比較画像合成（幅1080pxベース、JPEG品質92%、contain-fit）
- `createComparisonVideo`：2本の動画をCanvasに描画し captureStream + MediaRecorder で比較動画を合成（トリミング・contain-fit・進捗通知対応）
- `shareOrDownloadImage` / `shareOrDownloadVideo`：Navigator Share API またはダウンロードリンクによる保存・共有
- `cropTo3x4`：3:4クロップユーティリティ

### UI方針

- モバイルファースト（最大幅430px）
- ダークモード対応（Tailwind dark:クラス）
- グラデーション背景と装飾アニメーション
- タッチ操作最適化（ピンチズーム、ドラッグ）

## データ型定義

```typescript
type SubscriptionStatus = 'trialing' | 'active' | 'canceled' | 'past_due' | 'expired' | 'none'

interface SessionImage {
  id: string
  blob: Blob
  timestamp: number
}

type CompareFrameRatio = '3:4' | '9:16' | '4:3'
type CompareLayout = 'horizontal' | 'vertical'

// 撮影・並べる画面のモード
type CaptureMode = 'photo' | 'video'
type CompareMode = 'photo' | 'video'

// グリッド表示状態
type GridMode = 'off' | 'white' | 'red'

interface CompareFrameSettings {
  ratio: CompareFrameRatio
  layout: CompareLayout
  borderEnabled: boolean
  borderWidth: number
  borderColor: string
  dividerWidth: number
  borderRadius: number
}

// 比較合成オプション（抜粋：トリミング範囲）
interface ComparisonOptions {
  // …レイアウト・フレーム・スケール・オフセット・グリッド設定…
  drawGrid?: boolean
  gridColor?: string
  firstStart?: number   // 1本目の開始秒
  firstEnd?: number     // 1本目の終了秒
  secondStart?: number  // 2本目の開始秒
  secondEnd?: number    // 2本目の終了秒
}
```

## 商品としての一言説明

ぴたカメは、好きな画像・動画を横に並べながら、同じ構図で撮影・比較・出力できるサブスクリプション型の比較カメラです。写真も動画も、変化や違いをそのまま伝える1枚・1本に仕上げられます。
